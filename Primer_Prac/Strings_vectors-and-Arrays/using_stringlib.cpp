#include <iostream>

/* using std::cin; using std::cout;
using std::endl; */

using namespace std;

int main(int argc, char const *argv[])
{
    //auto cannot take signed and unsigned into consideration.
    /* auto a = 0;
    for (auto a = 0; a < 10; a++);
    cout << 10 << endl;     //these can be used without mentioning their namespaces. */

   /*  auto ss = {"My name is Abhsihek Mishra"};   //std::initializer_list<char*> a;
    std::initializer_list<char*> a; */

    
    return 0;
}
