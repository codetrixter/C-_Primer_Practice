#include <iostream>

using namespace std;

int main(int argc, char const *argv[])
{
    //auto declaration
    /* int i = 0;
    const int ci = i;
    auto b = ci;
    b = 90;
    auto c = &ci;
    const auto f = i; */

    //exercise... which of them are allowed.
   /*  const int i = 42;
    auto j = i; const auto &k = i; auto *p = &i;    //how is *p const int *
    const auto j2 = i, &k2 = i; */
    
    return 0;
}
